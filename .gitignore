# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

.idea

# General
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon


# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# env files
*.env
.env.local
.env.*.local
!example.env

# Editor/Workspace
.vscode/
*.code-workspace
.devcontainer/
.history/
.vs/
.fleet/

# Azure local configs (may contain secrets)
.azure/
local.settings.json

# Secrets and certificates (private material)
.secrets/
secrets/
.certs/
certs/
*.pem
*.key
*.p12
*.pfx
*.gpg
*.asc

# Direnv and envrc variants
.envrc
.envrc.*
.envrc.local

# Logs and temp files
logs/
log/
*.log
tmp/
temp/
*.tmp
*.swp
*.swo

# Windows-specific
Thumbs.db
ehthumbs.db
Desktop.ini

# Go build outputs and profiles
bin/
build/
dist/
coverage/
*.cover
*.coverprofile
*.pprof
*.prof

# Docker overrides (local-only)
docker-compose.override.yml
docker-compose.override.yaml
compose.override.yml
compose.override.yaml

# Sublime/other editor files
*.sublime-workspace
*.sublime-project